extend = "ruff.base.toml"

[lint.extend-per-file-ignores]
# chmod'ing the installed Linux executable to 0o755 before launch is the intended permission
"src/unity_devkit/install.py" = ["bad-file-permissions"]
